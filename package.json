{
  "name": "greenpixel-web",
  "version": "0.2.1",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/build.mjs && node scripts/serve.mjs",
    "test": "node --test",
    "build": "node scripts/build.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "GPL-3.0-only"
}
